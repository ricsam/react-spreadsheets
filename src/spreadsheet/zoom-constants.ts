export const ZOOM_FACTOR = 0.05; // Normal zoom speed
export const CONTROLLED_ZOOM_FACTOR = 0.01; // Slower zoom when holding opt/alt

export const DEFAULT_ZOOM = 1;
export const MIN_ZOOM = 0.5;
export const MAX_ZOOM = 3;
