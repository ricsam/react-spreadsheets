// Grid configuration - now infinite
// Note: COLUMNS and ROWS arrays removed in favor of on-demand generation

// Default dimensions
export const DEFAULT_CELL_WIDTH = 100;
export const DEFAULT_CELL_HEIGHT = 30;
export const HEADER_WIDTH = 50;
export const HEADER_HEIGHT = 30;
